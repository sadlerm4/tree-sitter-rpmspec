===============================================================================
Preamble Section (String)
===============================================================================

Name:           tree-sitter-rpmspec

-------------------------------------------------------------------------------

(spec
  (preamble
    (tags
      (tag)
      (string
        (string_content)))))


===============================================================================
Preamble Section (String with Expansions)
===============================================================================

Source:         %{url}/-/archive/main/%{name}-main.tar.gz

-------------------------------------------------------------------------------

(spec
  (preamble
    (tags
      (tag)
      (string
       (expansion
         (variable_name))
       (string_content)
       (expansion
         (variable_name))
       (string_content)))))
