===============================================================================
Files (Empty)
===============================================================================

%files

-------------------------------------------------------------------------------

(spec
  (files))

===============================================================================
Files (Simple Path)
===============================================================================

%files
/usr/bin/tree-sitter

-------------------------------------------------------------------------------

(spec
  (files
    (file
      (string
        (string_content)))))

===============================================================================
Files (Expansion)
===============================================================================

%files
%{_mandir}/*/*

-------------------------------------------------------------------------------

(spec
  (files
    (file
      (string
        (macro_expansion
          (variable_name))
      (string_content)))))

===============================================================================
Files (-n)
===============================================================================

%files -n subpackage
/usr/bin/tree-sitter

-------------------------------------------------------------------------------

(spec
  (files
    (string
      (string_content))
    (file
      (string
        (string_content)))))

===============================================================================
Files (-f)
===============================================================================

%files -f files.list

-------------------------------------------------------------------------------

(spec
  (files
    (string (string_content))))

===============================================================================
Files (%defattr)
===============================================================================

%files
%defattr(-,root,root)
/usr/bin/file

-------------------------------------------------------------------------------

(spec
  (files
    (defattr)
    (file
      (string (string_content)))))

===============================================================================
Files (%attr)
===============================================================================

%files
%attr(-, root, root) /usr/bin/file

-------------------------------------------------------------------------------

(spec
  (files
    (file
      (attr)
      (string
        (string_content)))))

===============================================================================
Files (%attr + %dir)
===============================================================================

%files
%attr(0770,root, group) %dir /path/

-------------------------------------------------------------------------------

(spec
  (files
    (file
      (attr)
      (string
        (string_content)))))

===============================================================================
Files (%config)
===============================================================================

%files
%config /etc/tree-sitter.conf

-------------------------------------------------------------------------------

(spec
  (files
    (file
      (string
        (string_content)))))

===============================================================================
Files (%doc)
===============================================================================

%files
%doc README NEWS

-------------------------------------------------------------------------------

(spec
  (files
    (file
      (string
        (string_content)))))

===============================================================================
Files (%dir)
===============================================================================

%files
%dir /usr/bin/

-------------------------------------------------------------------------------

(spec
  (files
    (file
      (string
        (string_content)))))

===============================================================================
Files (%dir with variables)
===============================================================================

%files
%dir %{_bindir}

-------------------------------------------------------------------------------

(spec
  (files
    (file
      (string
        (macro_expansion
          (variable_name))))))

===============================================================================
Files (%license)
===============================================================================

%files
%license LICENSE
%license NOTICES.txt

-------------------------------------------------------------------------------

(spec
  (files
    (file
      (string
        (string_content)))
    (file
      (string
        (string_content)))))

===============================================================================
Files (%license with multiple files)
===============================================================================

%files
%license LICENSE NOTICES.txt

-------------------------------------------------------------------------------

(spec
  (files
    (file
      (string
        (string_content)))))

===============================================================================
Files (%verify)
===============================================================================

%files
%verify(not size filedigest mtime) %{prefix}/bin/file

-------------------------------------------------------------------------------

(spec
  (files
    (file
      (verify)
      (string
        (macro_expansion
          (variable_name))
        (string_content)))))

===============================================================================
Files (%ifarch)
===============================================================================

%files
%ifarch x86_64
%{_bindir}/file
%endif

-------------------------------------------------------------------------------

(spec
  (files
    (ifarch_statement
      (arch
        (identifier))
      (file
        (string
          (macro_expansion
            (variable_name))
          (string_content))))))
